  apiVersion: v1
  kind: Service
  metadata:
    name: config-server-service
    namespace: digital-payment

  spec:
    selector:
      app: config-server

    ports:
      - port: 9999
        targetPort: 9999

    type: ClusterIP