  apiVersion: v1
  kind: Service
  metadata:
    name: auth-service-service
    namespace: digital-payment

  spec:
    selector:
      app: auth-service

    ports:
      - port: 8080
        targetPort: 8080

    type: NodePort